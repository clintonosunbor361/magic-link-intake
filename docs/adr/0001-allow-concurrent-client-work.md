# Allow concurrent Enquiries and Active Orders

Kuartz permits a person to have multiple open Enquiries and a Client to have multiple simultaneous Active Orders because distinct events and conversations can overlap. Identity matches warn and require acknowledgment, but never auto-merge people or block legitimate work.
